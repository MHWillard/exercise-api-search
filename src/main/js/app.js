const React = require('react');
const ReactDOM = require('react-dom');
const client = reqquire('./client');

class App extends React.Component {
    constructor(props) {
        super(props)
        this.state = {entry: []}
    }

    componentDidMount() {
        
    }
}